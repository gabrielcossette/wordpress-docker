--- docker-entrypoint.sh	2018-01-14 22:39:22.192153867 -0500
+++ entrypoint_modif	2018-01-14 22:39:16.932205711 -0500
@@ -93,6 +93,10 @@
 		"${uniqueEnvs[@]/#/WORDPRESS_}"
 		WORDPRESS_TABLE_PREFIX
 		WORDPRESS_DEBUG
+		SITE
+		USER
+		PASS
+		EMAIL
 	)
 	haveConfig=
 	for e in "${envs[@]}"; do
@@ -235,6 +239,14 @@
 EOPHP
 	fi
 
+    wp core install --url=$SITE --title="Site Web" --admin_user=$USER --admin_password=$PASS --admin_email=$EMAIL
+
+	wp plugin install wp-clone-by-wp-academy --activate
+
+	wp plugin install nginx-helper --activate
+
+	wp plugin install redis-cache --activate
+
 	# now that we're definitely done writing configuration, let's clear out the relevant envrionment variables (so that stray "phpinfo()" calls don't leak secrets from our code)
 	for e in "${envs[@]}"; do
 		unset "$e"
